apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    app: lab2
    component: db
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: lab2
    component: db
