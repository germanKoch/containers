apiVersion: v1
kind: Service
metadata:
  name: web-service
  labels:
    app: lab2
    component: web
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: lab2
    component: web
